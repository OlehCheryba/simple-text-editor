import { Action } from "redux";

const initialState = {

};

const textEditorReducer = (store = initialState, action: Action) => {
  switch (action.type) {
    default:
      return store;
  }
};

export default textEditorReducer;